val a = [1, 2, 3]

export val b = a.toString()

export val [c, d] = a

export enum Size { S, M, L, XL, Custom(size: String) }

export type Shirt {
  color: String
  size: Size
}

export func greet(greeting: String) = println(greeting + " world")

export enum Direction {
  Up
  Angled(degs: Float)
}
