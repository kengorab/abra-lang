// TODO: This shouldn't stackoverflow
//println([])

val arr = [1, 2, 3]

func makeAdder(x: Int[]): (Int) => Int {
  val f = (i: Int) => i + x.length

  println(x)

  f
}
val addOne = makeAdder(arr)

arr.pop()

/// Expect: 12
println(addOne(11))
