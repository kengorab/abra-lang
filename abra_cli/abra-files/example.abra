// TODO: This shouldn't stackoverflow
//println([])

val arr = [1, 2, 3, 4]
//func sum(acc: Int, i: Int): Int = acc + i
//println(arr.reduce(0, (acc, i) => acc + i))

val fn = (x: Int) => x + 1
println(fn(123))
