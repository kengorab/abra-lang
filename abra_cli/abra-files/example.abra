func getCounter() {
  var count = 100

  func unnecessaryLayer1() {
    func unnecessaryLayer2() {
      func tick() {
        count = count + 1
      }

      tick
    }

    unnecessaryLayer2
  }

  count = 0
  val l2 = unnecessaryLayer1()
  l2()
}

val tick = getCounter()
println(tick())
println(tick())
println(tick())
