type Person {
  name: String
  age: Int
}

val p1 = Person(name: "Ken", age: 31)
val p2 = Person(name: "Ken", age: 32)
println(#{p1, p1, p2})

// TODO: This should work
//func makeArray<U>(u: U): U[] = [u]
//func f<T>(t: T): T[] = makeArray(t)
// TODO: So should this
//func makeArray<U>(): U[] = []
//type Foo<T> {
//  a: T[] = makeArray()
//}
