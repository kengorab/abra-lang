// TODO: This shouldn't stackoverflow
//println([])

enum Color {
  Red
  Blue
  Green
  RGB(r: Int, g: Int, b: Int)
}

val c = Color.RGB(r: 1, g: 2, b: 3)
match c {
  Color.RGB(r, g, b) c => {
    c.b = 100
  }
  _ => {}
}
c
