type Person {
  name: String
  age: Int

  func greet(self): String {
    self.name + self.age
  }
}

func foo(i: Int): Int { i + 1 }

func bar(x: Bool, y: String) { }

// TODO: This should work
//func makeArray<U>(u: U): U[] = [u]
//func f<T>(t: T): T[] = makeArray(t)
// TODO: So should this
//func makeArray<U>(): U[] = []
//type Foo<T> {
//  a: T[] = makeArray()
//}
