// TODO: Make this work
// func foo(a: Int, b: Bool, c = bar()): Int = -1
// func bar(): Int = 123

//func foo<T>(a: T[]): T[] = a
//val x = foo(foo([1]))

//val x = a => "asdf"

[1, 2, 3].map((x, y = 12) => x)

val arr = ["a", "bc", "def"]
arr.forEach(s => {

})

//val sizes: Int[] = arr.map((s: Int) => println(s))
//val sizes: Int[] = arr.map(s => println(s))

//val sizes: Int[] = arr.map((s: Int) => 1.23)
//val _: Int = sizes
