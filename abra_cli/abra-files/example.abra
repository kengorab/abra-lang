type A {
  a: String
}

type Person {
  name: A

  func getName(self) = self.name
}

val ken = Person(name: A(a: "ken"))
val a = ken.name = A(a: "Ken")
println(ken.name.a)
println(a.a)
a.a = "asdf"
println(ken.name.a)
