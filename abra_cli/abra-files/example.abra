// TODO: This shouldn't stackoverflow
//println([])

enum Color {
  Red
  Blue
  Green
  RGB(r: Int, g: Int, b: Int)
}

val arr = [Color.Red, Color.Blue, Color.Green, Color.RGB(r: 1, g: 2, b: 3)]

val n = match arr[3] {
  None => "none"
  Color.Red => "red"
  Color.Blue => "blue"
  Color.Green => "green"
  Color.RGB(r, g, b) => "rgb($r, $g, $b)"
}
n
