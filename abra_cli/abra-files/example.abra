var f: () => Int
for i in [1, 2, 3, 4, 5] {
  val x = i

  f = () => x
  if true break
}
println(f())
















/* TODO: Function hoisting
val hello = "Hello"

type Person {
  name: String

  func greet(self) {
    sayHello(hello)
  }
}

func sayHello(msg: String) = println(msg)

val me = Person(name: "Ken")
me.greet()
*/
