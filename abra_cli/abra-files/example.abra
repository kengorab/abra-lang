val arr = range(0, 15)
println("Array: " + arr)

var iterations = 0

func fib(n: Int): Int {
  iterations = iterations + 1

  if n == 0 {
    0
  } else if n == 1 {
    1
  } else {
    fib(n - 2) + fib(n - 1)
  }
}

println("Iterations: " + iterations)
fib(8)
