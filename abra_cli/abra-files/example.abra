// TODO: This shouldn't stackoverflow
//println([])

//val r = range(0, 5, 2)
//
//println(r.next())
//println(r.next())
//println(r.next())
//println(r.next())
//println(r.next())

for i in range(0, 5, 2) {
  println(i)
}

println("done")
