val message = "Hello World"

type A {
  func abc(): String = "Hello!"
  func def(): String = message + "!"
}

println(A.abc, A.abc(), A.def, A.def())

println(Map.fromPairs, Map.fromPairs([("a", 2), ("b", 3)]))

/* TODO: Function hoisting
val hello = "Hello"

type Person {
  name: String

  func greet(self) {
    sayHello(hello)
  }
}

func sayHello(msg: String) = println(msg)

val me = Person(name: "Ken")
me.greet()
*/
