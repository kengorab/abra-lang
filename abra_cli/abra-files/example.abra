type Fn {
  f: (Int) => Int

  func call(self, i: Int): Int = self.f(i)
}

func incr(x: Int): Int = x + 1

val add1 = Fn(f: incr)
println(add1.call(1))




/* TODO: Function hoisting
val hello = "Hello"

type Person {
  name: String

  func greet(self) {
    sayHello(hello)
  }
}

func sayHello(msg: String) = println(msg)

val me = Person(name: "Ken")
me.greet()
*/
