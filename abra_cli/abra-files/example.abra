type Person {
  firstName: String
  lastName: String

  func fullName(self) {
    self.firstName + " " + self.lastName
  }
}

val ken = Person(firstName: "Ken", lastName: "Gorab")
println(ken.fullName())
