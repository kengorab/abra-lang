// TODO: This shouldn't stackoverflow
//println([])

enum Color {
  Red
  Blue
  Green
  RGB(r: Int, g: Int, b: Int)
}

val arr = [Color.Red, Color.Blue, Color.Green]

val n = match arr[1] {
  None => "none"
  Color.Red => "red"
  Color.Blue => "blue"
  Color.Green => "green"
  Color.RGB => "rgb"
}
n
