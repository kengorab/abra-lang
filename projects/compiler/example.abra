import "process" as process

func foo() {
  print("hello ")
  bar()
}

func bar() {
  print("world")
  baz()
}

func baz() {
  println("!")
  println(process.callstack())
}

val arr = [1].map((i, _) => {
  foo()
  i + 1
})
