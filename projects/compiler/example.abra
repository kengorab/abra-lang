import stringify from "meta"
import debug, println, assert from "./example2"

val x = 123
val y = debug(x) + 1
println("y: $y", "a", x)
println("${stringify(x)} = $x")

func foo() {
  val arr = [1, 2]
  val v = arr.length > 4
  assert(v)
}
foo()
println("afterwards")
