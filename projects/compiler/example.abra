enum Foo {
  Bar
  Baz
  Qux
}

// func foo(f: Foo) {
//   val s = match f {
//     Foo.Bar => 1
//     Foo.Baz(s1, s2) => {
//       println(s1, s2)
//       s1.length + s2.length
//     }
//     Foo.Qux => 2
//   }
//   println(s)
// }

// foo(Foo.Baz("abc", "de"))

func foo(f: Foo): Foo {
  match f {
    Foo.Bar => return Foo.Bar
    Foo.Baz => return Foo.Bar
    Foo.Qux => return Foo.Qux
  }
}

stdoutWriteln(foo(Foo.Bar).toString())

// stdoutWriteln("hello")


