var cond = true
// val arr = [1, 2, 3]

var y = 1 + 1
val x = match y { //arr[0] {
  1 => if cond {
    123
  } else {
    unreachable("asdf")
  }
  // 1 => 123
  else => 0
}
stdoutWriteln(x.toString())
