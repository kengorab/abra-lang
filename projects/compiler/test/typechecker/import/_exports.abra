pub val a = 123

pub func add(a: Int, b: Int): Int = a + b

pub type Foo {
  color: Color

  func foo(self): Int = 123
}
pub val foo = Foo(color: Color.Green)

pub enum Color {
  Red
  Green
  Blue
  RGB(r: Int, g: Int, b: Int)
}
